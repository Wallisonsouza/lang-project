// 1. Função sem nome
fn (a: Number) {
  val x: Number = 10
}

// 2. Parêntese não fechado
fn soma(a: Number, b: Number {
  val x = a + b
}

// 3. Parâmetro sem identificador
fn soma(: Number) {
}

// 4. Parâmetro sem tipo
fn soma(a:) {
}

// 5. Parâmetro com erro múltiplo
fn soma(a: , b Number) {
}

// 6. Vírgula extra
fn soma(a: Number,, b: Number) {
}

// 7. Retorno sem tipo
fn soma(a: Number) -> {
  return a
}

// 8. Corpo ausente
fn soma(a: Number) -> Number

// 9. Chave não fechada
fn soma(a: Number) -> Number {
  val x = 10

// 10. Função válida depois de erros
fn ok(a: Number, b: Number) -> Number {
  return a + b
}
